Julia implementation of pairing heaps. 
Used because supports amortized time per delete-min in O(log n) and
the operations find-min, merge, and insert run in O(1) amortized time.
Also lightweight as compared to financial heaps because of 
2 pointers isntead of 4 like fibonacci heaps.
